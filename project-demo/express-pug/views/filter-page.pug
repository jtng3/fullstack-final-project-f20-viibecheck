doctype html
html
  head
    title #{title} - My Site
    link(rel='stylesheet', href='/styles.css')
  body
    header
      h1 REST Countries
      
    main
      div 
        each region in regions
          h3 #{region}
          ol
            each country in filter[region]
              li #{country.name} - #{country.population}
          